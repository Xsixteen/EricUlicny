{% extends 'base.twig' %}


{% block precontent %}
 
{% endblock %}

{% block content %}
  {# DEBUG -- dump all variables
  <section> <pre>{{ dump() }}</pre> </section>
  #}
  <section id="section-posts">
    <div class="container">
    
      <div class="row">
        <div class="col-xs-12">
          <div class="page-header">
            <h1 id="news">Latest News <small>This is a Stub!</small></h1>
          </div>
        </div>
      </div>
      <div class="row">

      {% if projects|length > 0 %}
        <ul>
          {% for project in projects %}
            <li>
              <b>{{ project.name }}</b><br/>
              {{ project.description }}
            </a>
            </li>
          {% endfor %}
        </ul>
        {% else %}
          No Projects!
        {% endif %}
      </div>

    </div>
  </section>
{% endblock %}